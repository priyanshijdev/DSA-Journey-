Leetcode:125
Input: s = "A man, a plan, a canal: Panama"
Output: true
Explanation: "amanaplanacanalpanama" is a palindrome.

code:

    bool isPalindrome(string s) {
        int l=0, e=s.length()-1;

        while(l<e){
            if(!isalnum(s[l])){
                l++;
            }
            else if(!isalnum(s[e])){
                e--;
            }
            else if(tolower(s[l])!=tolower(s[e])) 
              {  return false;}
            else {
                l++, e--;
            }
        }
        return true;
