Input: nums = [100,4,200,1,3,2]
Output: 4
subs would be one after another 

Approach :
use Sorting 
1 sort 
2 run loop i= 1 to n
3 compare if( a[i] != a[i-1])
     if(a[i] == a[i]+1)  // 2 == 1+1
  count+=1
4 else {
   longest=max(longest, count);
   count=1;
}
5 return max(longest,count);
-----------------------------------------------------------------------------------------------------
